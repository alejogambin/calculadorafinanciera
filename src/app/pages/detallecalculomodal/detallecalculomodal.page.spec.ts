import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DetallecalculomodalPage } from './detallecalculomodal.page';

describe('DetallecalculomodalPage', () => {
  let component: DetallecalculomodalPage;
  let fixture: ComponentFixture<DetallecalculomodalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DetallecalculomodalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
